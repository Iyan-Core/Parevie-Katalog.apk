import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.katalogpro.app',
  appName: 'KatalogPro',
  webDir: 'dist',
  server: {
    cleartext: true,     // <-- BARIS INI YANG PENTING
    androidScheme: 'https'
  },
};

export default config;
